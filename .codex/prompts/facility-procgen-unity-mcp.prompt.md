# Facility Procgen Unity MCP Prompt

Use this prompt when the task should be carried out through Unity MCP.

```text
Use the unity-mcp-orchestrator skill and the project routing in AGENTS.md. Work in the active Unity project through MCP.

Read editor state, inspect the relevant scene hierarchy/assets/components, then make the requested additive changes. After edits, check compilation and console errors. Use screenshots or hierarchy checks when visual scene correctness matters.
```

